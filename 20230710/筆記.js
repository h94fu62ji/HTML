length // len
toUpperCase() // Upper
toLowerCase() // Lower
charAt(1) // 搜尋字串位置[1]
indexOf() // 字串找位置index()
substring(0,5) // 字串打印位置[0:5]
// > 
// <
// >=
// <=
// == 相等 但3跟"3"相等
// === 完全相等
// != 不相等 但3跟"3"相等
// !== 全不相等
// & 回傳數字 只能用符號
// && 回傳 T F
Math.mix() // 找最大
Math.min() // 找最小
Math.pow(2,3) // 次方 2**3
Math.sqrt(36) // 根號 36**0.5
Math.round() // 四捨五入
Math.random() // 隨機浮點數 0~0.99999
Math.round(Math.random()*x+y) //x範圍 y起始位置

parseInt() //轉換成整數 Int
parseFloat() //轉換成浮點 Float